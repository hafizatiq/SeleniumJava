package Helper;
import org.openqa.selenium.*;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;



public class HelperClass {

	static WebDriver driver;
	static WebDriverWait wait;
	
	/*
	 * public HelperClass(WebDriver driver) { this.driver = driver; wait = new
	 * WebDriverWait(driver, 10); }
	 * 
	 * public static WebElement waitforElementByVisibility(WebElement locator) {
	 * 
	 * return wait.until(ExpectedConditions.visibilityOf(locator)); }
	 * 
	 * public static WebElement waitForElementPresence(By locator) {
	 * 
	 * return wait.until(ExpectedConditions.presenceOfElementLocated(locator)); }
	 * 
	 * public static WebElement getElement(By locator) {
	 * 
	 * return driver.findElement(locator); }
	 * 
	 * public static void setValue(String locator, String value) {
	 * 
	 * driver.findElement(By.id(locator)).sendKeys(value); }
	 * 
	 * public static void clickOnElement(By locator) {
	 * driver.findElement(locator).click(); }
	 */
	
}
